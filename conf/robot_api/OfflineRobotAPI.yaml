defaults:
  - _self_

_target_: asp_agent.robot_api.OfflineRobotAPI.OfflineRobotAPI

dataset: ${dataset}

dataloader:
  _target_: torch.utils.data.DataLoader
  _partial_: True
  batch_size: 1
  shuffle: false
  num_workers: 2
  pin_memory: false
  collate_fn:
    _target_: rgbd_dataset.rgbd_dataset.dataloader.collate_fn_np_single_cb
